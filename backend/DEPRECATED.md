# DEPRECATED

This directory contains the original pre-Lambda Express/WebSocket server.
It has been fully replaced by the AWS Lambda architecture in `lambda/` and `terraform/`.

Do not deploy this server. It is retained for historical reference only.
